#include "includes/cub3d.h"

void ft_resolution(char *str)
{
   int i;

    if (str[i] == R)
    {
        while(str[i++] == \0)
        if (ft_isdigit(str[i]))
            if (data->axex == 0)
                data->axex = ft_atoi(str[i]);
            else
                (data->axey = ft_atoi(str[i]));
    }
}